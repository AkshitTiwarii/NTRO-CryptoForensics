# 🚀 Quick Push Commands - Copy & Paste

## Option 1: Using Your GitHub Username (AkshitTiwarii)

```powershell
# Navigate to project
cd C:\Users\akshi\OneDrive\Desktop\PROJECTS\Cryptodata\CryptoData

# Add remote (replace REPO_NAME with your choice)
git remote add origin https://github.com/AkshitTiwarii/NTRO-CryptoForensics.git

# Rename branch to main
git branch -M main

# Push to GitHub
git push -u origin main
```

## Option 2: Create New Repository First

1. **Go to GitHub:** https://github.com/new
2. **Create repo:** Name it `NTRO-CryptoForensics` (Private recommended)
3. **DO NOT** initialize with README, .gitignore, or license
4. **Copy the URL** GitHub shows (e.g., `https://github.com/AkshitTiwarii/NTRO-CryptoForensics.git`)
5. **Run these commands:**

```powershell
cd C:\Users\akshi\OneDrive\Desktop\PROJECTS\Cryptodata\CryptoData
git remote add origin YOUR_COPIED_URL
git branch -M main
git push -u origin main
```

## Verify Status Before Pushing

```powershell
# Check what's committed
git log --oneline -1

# Check branch
git branch

# Check remote
git remote -v
```

## After First Push - Future Updates

```powershell
# Make changes, then:
git add .
git commit -m "Update: description of changes"
git push
```

## Troubleshooting

### Remove wrong remote:
```powershell
git remote remove origin
```

### Force push (use carefully):
```powershell
git push -u origin main --force
```

### Check what will be pushed:
```powershell
git log origin/main..HEAD
```

---

**Current Status:**
- ✅ Repository initialized
- ✅ 138 files committed (45,215 lines)
- ✅ Commit: `3235b52`
- ✅ Branch: `master` (will rename to `main`)
- ⏳ Ready to push to GitHub!
